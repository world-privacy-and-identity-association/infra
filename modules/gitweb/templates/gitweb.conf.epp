our $projectroot = "/srv/git";
our $project_maxdepth = 1;
our $GIT = "/usr/bin/git";
our @git_base_url_list = qw(https://code.<%=$systemDomain%> git://code.<%=$systemDomain%>);
our $site_name = "WPIA source code repositories";
our $omit_owner = true; # all repositories are owned by the git user – useless information
our $default_text_plain_charset = "utf-8";
our $fallback_encoding = "utf-8";
our $projects_list_description_width = 50;
